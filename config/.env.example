# Copy to config/.env and adjust as needed.
# .env is gitignored; this example IS committed so others know what to set.
BASE_URL=https://automationexercise.com
API_PREFIX=/api
TIMEOUT=30
LOG_LEVEL=INFO
